# تطبيق المدرس — Teacher App (Flutter)

> تطبيق موبايل بسيط للمدرس، يدير حلقاته وطلابه بسلاسة،
> مع دعم **Offline-First** ومزامنة تلقائية عند توفر الإنترنت.

---

## 1. هيكل التطبيق (App Structure)

```
تطبيق المدرس
│
├─ 1. شاشة الدخول  (Login)
│
├─ 2. الشاشة الرئيسية (Home)
│   ├─ اختيار الدورة
│   ├─ اختيار الحلقة
│   └─ أزرار الإجراءات
│       ├─ حضور/غياب (دفعة)
│       ├─ حفظ (دفعة)
│       ├─ طلب سبر
│       └─ سجل الطالب
│
├─ 3. حضور/غياب (Batch)
│   ├─ اختيار التاريخ
│   ├─ قائمة الطلاب (اختيار الحالة لكل طالب)
│   └─ حفظ (محلي + مزامنة)
│
├─ 4. حفظ (Batch)
│   ├─ اختيار التاريخ
│   ├─ لكل طالب: سورة + آيات + نوع + نتيجة
│   └─ حفظ
│
├─ 5. طلب سبر
│   ├─ اختيار الطالب
│   ├─ اختيار الجزء + النوع
│   └─ إرسال
│
├─ 6. سجل الطالب
│   ├─ ملخص نقاط وحضور وحفظ وسبرات
│   └─ سجل زمني
│
└─ 7. إعدادات
    ├─ الملف الشخصي
    ├─ المزامنة
    └─ تسجيل خروج
```

---

## 2. شاشة الدخول (Login)

```
┌─────────────────────────────────┐
│                                 │
│         [شعار التطبيق]          │
│     نظام إدارة الحلقات          │
│                                 │
│  ┌─────────────────────────┐   │
│  │  اسم المستخدم           │   │
│  └─────────────────────────┘   │
│  ┌─────────────────────────┐   │
│  │  كلمة المرور            │   │
│  └─────────────────────────┘   │
│                                 │
│  ┌─────────────────────────┐   │
│  │       دخول              │   │
│  └─────────────────────────┘   │
│                                 │
│  حفظ الجلسة  [✅]              │
│                                 │
│  نسخة 1.0                      │
└─────────────────────────────────┘
```

**API:** `POST /api/token/` ← `{username, password}` → `{access, refresh}`
**Offline:** يخزن `access_token` في **SharedPreferences** + **encrypted storage**

---

## 3. مؤشر حالة المزامنة (Sync Status Bar)

> شريط ثابت في أعلى التطبيق (أسفل الـ Header) يظهر حالة المزامنة دائماً.

```
┌──────────────────────────────────────────────────┐
│  📚 الدورة الصيفية 1447   ⭕ حلقة الفجر          │
├──────────────────────────────────────────────────┤
│  🔄 مزامنة تلقائية    ● متصل   2 غير متزامن     │
│  [⬆️ 3 حضور]  [⬆️ 2 حفظ]  [📤 1 طلب سبر]      │
├──────────────────────────────────────────────────┤
│                                                  │
│           (محتوى الشاشة الرئيسية)                 │
│                                                  │
```

### حالات المؤشر

| الحالة | اللون | الشكل | المعنى |
|--------|-------|-------|--------|
| 🟢 متصل + محدث | أخضر | `● متصل ✓` | كل شيء متزامن |
| 🟡 متصل + معلق | أصفر | `● متصل ⬆️ 3` | يوجد تغييرات تنتظر الإرسال |
| 🔴 غير متصل | أحمر | `○ غير متصل (2 معلق)` | لا يوجد إنترنت — تعمل من المحلي |
| 🔵 جاري المزامنة | أزرق | `🔄 جاري المزامنة...` | ترسل البيانات حالياً |
| ⚠️ خطأ في المزامنة | برتقالي | `⚠️ فشل لـ 1 — اضغط لإعادة` | تعذر الإرسال |

### كيف يعمل الـ SyncService في الخلفية

```
┌─────────────────────────────────────┐
│  المشغل (Trigger)                   │
│                                     │
│  1. التطبيق في المقدمة (Foreground) │
│     - عند حفظ أي إجراء ← يحفز sync │
│     - Timer كل 30 ثانية             │
│     - عند عودة الاتصال (connectivity│
│      _stream)                      │
│                                     │
│  2. التطبيق في الخلفية (Background) │
│     - WorkManager (Android)         │
│     - BGTaskScheduler (iOS)         │
│     - كل 15 دقيقة (أو حسب سياسة OS)│
│                                     │
│  3. التطبيق مغلق تماماً             │
│     - WorkManager periodic task     │
│     - يستيقظ → يتحقق من الإنترنت    │
│     - يرسل pending data → API       │
│     → يغلق (حتى المرة القادمة)      │
└─────────────────────────────────────┘
```

### WorkManager (Android)

```dart
// التسجيل (مرة واحدة عند بدء التشغيل)
Workmanager().registerPeriodicTask(
  "sync-task",
  "backgroundSync",
  frequency: Duration(minutes: 15),
  constraints: Constraints(
    networkType: NetworkType.CONNECTED,  // فقط مع الإنترنت
  ),
  existingWorkPolicy: ExistingWorkPolicy.KEEP,
);

// الـ CallbackDispatcher
@pragma('vm:entry-point')
void callbackDispatcher() async {
  WidgetsFlutterBinding.ensureInitialized();
  await SyncService().syncAll();  // ← يرسل كل المعلق
}

// iOS (BGTaskScheduler)
BGTaskScheduler.register(
  "com.halaqat.sync",
  (task) async {
    await SyncService().syncAll();
    task.setTaskCompleted(success: true);
  },
);
```

### الـ Sync Queue (طابور المزامنة)

```
تم حفظ الإجراء محلياً
    │
    ▼
يُضاف إلى SyncQueue (جدول SQLite)
    │
    ▼
كل إجراء له حالة:
  pending    ← لم يُرسل بعد
  sending    ← جاري الإرسال حالياً
  synced     ← تم بنجاح
  failed     ← فشل (سيُعاد لاحقاً)
    │
    ▼
مزامنة:
  1. قراءة جميع pending من SQLite
  2. تجميعها حسب (type, date, circle) للـ batch
  3. إرسال batch API
  4. نجاح → تحديث الحالة إلى synced
  5. فشل → زيادة retry_count
      - retry_count < 5: يبقى pending
      - retry_count >= 5: يصبح failed (يحتاج تدخل المستخدم)
```

---

## 4. الشاشة الرئيسية (Home)

```
┌─────────────────────────────────┐
│  📚 الدورة:  الدورة الصيفية  ▼  │
│  ⭕ الحلقة:  حلقة الفجر      ▼  │
├─────────────────────────────────┤
│  🔄 مزامنة تلقائية  🟢 متصل ✓  │
├─────────────────────────────────┤
│                                 │
│  [✅ حضور/غياب]  [✏️ حفظ دفعة] │
│                                 │
│  [📝 طلب سبر]     [👤 سجل طالب]│
│                                 │
│  ═════════════════════════════  │
│  آخر النشاطات (آخر 5):          │
│                                 │
│  • 22-05  محمد  ✅ حضور        │
│  • 22-05  علي   ✏️ حفظ-الفاتحة│
│  • 21-05  سعيد  📝 سبر-الأول   │
│  • 21-05  خالد  ✅ حضور        │
│                                 │
│  ═════════════════════════════  │
│  إحصائيات سريعة:                │
│                                 │
│  👥 25 طالب  │  ✅ 230 حضور    │
│  ✏️ 45 حفظ   │  📊 12 سبر     │
│                                 │
├─────────────────────────────────┤
│  [🏠]  [📊]  [👤]  [⚙️]        │
└─────────────────────────────────┘
```

**API:**
- `GET /api/circles/?course=1` ← تحميل الحلقات
- `GET /api/enrollments/?circle=1` ← تحميل طلاب الحلقة
- `GET /api/dashboard/teacher-dashboard/` ← إحصائيات سريعة
- `GET /api/attendance/stats/?course=1` ← إحصائيات الحضور

**Offline:**
- تخزين آخر حالة للدورة والحلقة في **SharedPreferences**
- تخزين الطلاب في قاعدة **SQLite محلية** للمقارنة عند المزامنة

### تدفق اختيار الدورة والحلقة

```
[اختيار دورة ▼]
├── الدورة الصيفية 1447  ← اخترت هذه
├── الدورة الشتوية 1447
└── ...

[اختيار حلقة ▼]
├── حلقة الفجر  ← اخترت هذه (25 طالباً)
├── حلقة العصر  (18 طالباً)
└── حلقة المغرب  (22 طالباً)
```

بعد الاختيار: تظهر أزرار الإجراءات الأربعة ويتم تحميل طلاب الحلقة في الخلفية.

### واجهة طلابي (قائمة الطلاب + ترتيب + تفاصيل)

```
┌─────────────────────────────────┐
│  ← العودة                       │
│  👥 طلاب — حلقة الفجر (25)      │
│  🔍 [ابحث عن طالب............]  │
├─────────────────────────────────┤
│                                 │
│  [ترتيب: الأبجدي ▼]            │
│  ┌─────────────────────────┐   │
│  │ ▪️ أ. أحمد              │   │
│  │     حضور 15/20  نقاط 230│   │
│  │ ▪️ ب. بدر               │   │
│  │     حضور 12/20  نقاط 180│   │
│  │ ▪️ ج. جاسم              │   │
│  │     حضور 18/20  نقاط 310│   │
│  │ ▪️ م. محمد              │   │
│  │     حضور 20/20  نقاط 450│   │
│  └─────────────────────────┘   │
│                                 │
│  [⬆️ آخر النشاطات] [🔤 أبجدي] │
│  [📊 الأعلى نقاطاً] [✅ الأعلى  │
│   حضوراً]                     │
│                                 │
├─────────────────────────────────┤
│  [🏠]  [📊]  [👤]  [⚙️]        │
└─────────────────────────────────┘
```

**خيارات الترتيب (يتم من Flutter محلياً):**

| الخيار | الوصف | المنطق |
|--------|-------|--------|
| 🔤 أبجدي | A-Z حسب الاسم | `students.sort((a,b) => a.name.compareTo(b.name))` |
| ⬆️ آخر نشاط | الأحدث أولاً | `students.sort((a,b) => b.lastActivity.compareTo(a.lastActivity))` |
| 📊 الأعلى نقاطاً | تنازلياً حسب النقاط | `students.sort((a,b) => b.totalPoints.compareTo(a.totalPoints))` |
| ✅ الأعلى حضوراً | تنازلياً حسب نسبة الحضور | `students.sort((a,b) => b.attendanceRate.compareTo(a.attendanceRate))` |
| 🔴 الأكثر غياباً | تصاعدياً حسب الحضور | `students.sort((a,b) => a.attendanceRate.compareTo(b.attendanceRate))` |

> جميع هذه الترتيبات تتم **محلياً في Flutter** من البيانات المخبأة، بدون الحاجة لـ API جديد.
> فقط تأكد من أن `cached_students` تحتوي على `total_points` و `attendance_count` و `total_sessions`.

**API:** `GET /api/enrollments/?circle=1` ← يحمل كل طلاب الحلقة مع بياناتهم

### معالجة الطلاب المؤرشفين / المحذوفين

```
السيناريو 1: طالب أُرشِف (archived) ← الحالة تتغير إلى "archived"
─────────────────────────────────────────────────────────────
  قبل المزامنة:
    • التطبيق المحلي لا يعلم بالتغيير
    • الطالب يظهر في القائمة كالمعتاد
  
  عند المزامنة:
    • syncAll() يسحب GET /api/enrollments/?circle=1
    • يقارن مع cached_students المحلي
    • يجد أن الطالب الآن status=archived
    • يُحدّث cached_students في SQLite
    • التطبيق يعرض الطالب مع شارة "مؤرشف" (🔴 Archived)

  ✅ العرض في الواجهة:
    • الطالب يظهر في القائمة لكن بلون رمادي
    • بجانبه شارة "مؤرشف"
    • عند محاولة تسجيل حضور له → رسالة "الطالب مؤرشف"
    • دوره في الإحصائيات (نقاطه) لا يختفي — تبقى كبيانات تاريخية

السيناريو 2: طالب حُذف (deleted) ← التسجيل أزيل بالكامل
─────────────────────────────────────────────────────────────
  قبل المزامنة:
    • التطبيق المحلي لا يزال يظهر الطالب في القائمة
    • (هذا متوقع — البيانات المحلية قديمة)

  عند المزامنة (fetchLatestData):
    • GET /api/enrollments/?circle=1 → لم يعد الطالب موجوداً في response
    • المقارنة: الطالب في cached_students لكن ليس في response
    • هل نَحذفه من المحلي؟ → نعم، بعد 30 يوماً من الغياب
    • لا — بل نُبقيه مع sync_status='deleted_on_server'
    • بعد 30 يوماً نَحذفه من SQLite نهائياً

  ✅ العرض في الواجهة:
    • الطالب لا يظهر في القائمة الافتراضية (filter: نشط فقط)
    • لكنه يظهر مع فلترة "المؤرشفون" أو "الكل"
    • سجلاته السابقة (حضور، حفظ، سبرات) تبقى مرئية

السيناريو 3: بدون إنترنت والطالب محذوف من السيرفر
─────────────────────────────────────────────────────────────
  • cached_students تخزّن آخر حالة معروفة
  • التطبيق يعرض الطالب طبيعي — لا توجد طريقة لمعرفة الحذف
  • عند عودة الإنترنت → المزامنة تكشف الفرق → تحديث

السيناريو 4: الطالب انتقل إلى حلقة أخرى أو دورة أخرى
─────────────────────────────────────────────────────────────
  • الطالب لم يعد ضمن GET /api/enrollments/?circle=1
  • نفس معالجة الحذف — يختفي من القائمة بعد المزامنة
  • سجلاته السابقة (حضور، حفظ) تبقى في cached_attendance
    و cached_memorizations — لأنها مخزنة بمعرف الطالب
    وليس بمعرف الحلقة
```

---

## 5. شاشة الحضور والغياب (Batch Attendance)

```
┌─────────────────────────────────┐
│  ← العودة                       │
│  ✅ حضور/غياب — حلقة الفجر      │
│  🟢 متصل ✓  2 غير متزامن       │
│  📅 [23-05-2026  الخميس]        │
├─────────────────────────────────┤
│                                 │
│  تحديد الكل:  [✅] [❌] [⏸]    │
│                                 │
│  ┌─────────────────────────┐   │
│  │  1.  محمد               │   │
│  │      [✅] [❌] [⏸]      │   │
│  ├─────────────────────────┤   │
│  │  2.  علي                │   │
│  │      [✅] [❌] [⏸]      │   │
│  ├─────────────────────────┤   │
│  │  3.  خالد               │   │
│  │      [✅] [❌] [⏸]      │   │
│  ├─────────────────────────┤   │
│  │  ... (25 طالباً)        │   │
│  └─────────────────────────┘   │
│                                 │
│  ┌─────────────────────────┐   │
│  │      💾 حفظ الحضور      │   │
│  └─────────────────────────┘   │
│                                 │
│  ⚡ تم الحفظ محلياً - ينتظر     │
│  المزامنة (3 تغييرات)           │
│                                 │
├─────────────────────────────────┤
│  [🏠]  [📊]  [👤]  [⚙️]        │
└─────────────────────────────────┘
```

### Offline-first Flow للحضور

```
1. المستخدم يفتح شاشة الحضور
2. يختار التاريخ (افتراضي: اليوم)
3. تظهر قائمة الطلاب من SQLite المحلية
4. المستخدم يختار الحالة لكل طالب (present/absent/excused)
5. الضغط على "حفظ الحضور"
6. → حفظ محلياً في SQLite (جدول pending_attendance)
7. ← عرض "تم الحفظ محلياً" + تحديث مؤشر المزامنة
8. عند توفر الإنترنت: الـ SyncService يقرأ جدول pending_
   attendance ويرسل POST /api/attendance/batch/ أو POST فردي
9. بعد نجاح المزامنة: حذف من pending_attendance + تحديث local DB
```

### التعديل قبل المزامنة (Edit Before Sync)

```
المستخدم اختار حالة خطأ لطالب (مثلاً حاضر بدل غائب)
    │
    ├─ الإجراء لم يُزامن بعد (pending)
    │     │
    │     └─ المستخدم يفتح الحضور لنفس التاريخ
    │         → يرى علامة ⏳ بجانب السجلات غير المتزامنة
    │         → يعدل حالة الطالب
    │         → يحفظ ← يُحدّث الـ pending row في SQLite
    │         → المزامنة سترسل القيمة الجديدة
    │
    └─ الإجراء تزامن بالفعل (synced)
          │
          └─ المستخدم يفتح الحضور لنفس التاريخ
              → يرى علامة ✅ بجانب السجلات المتزامنة
              → يعدل حالة الطالب
              → يحفظ ← ينشئ pending row جديد (كـ "تعديل")
              → عند المزامنة:
                1. يرسل PATCH /api/attendance/{id}/ ← يعدل السجل في السيرفر
                2. OR يرسل POST /api/attendance/batch/ مرة أخرى
                   (لأن attendance batch يستخدم get_or_create حسب enrollment+date)
              → بعد النجاح: يحذف pending
```

**لماذا يعمل التعديل حتى بعد المزامنة؟**
- `Attendance` له unique constraint `(enrollment, date)`.
- إعادة إرسال `POST /api/attendance/batch/` بنفس الـ enrollment+date
  يقوم بـ `get_or_create` ويُحدّث `status` تلقائياً.
- لا حاجة لـ `PATCH` — البatch نفسه يتعامل مع التعديلات.

### سجل الحضور والغياب للحلقة

```
┌─────────────────────────────────┐
│  ← العودة                       │
│  📋 سجل الحضور — حلقة الفجر      │
│  فلترة: [📅 أسبوعي] [شهري] [مخصص]│
├─────────────────────────────────┤
│                                 │
│  📅 الأسبوع من 17-05 إلى 23-05  │
│                                 │
│  ┌──────┬──────┬──────┬──────┬─┐│
│  │ طالب │الاحد│الثلاثاء│الخميس│ ││
│  ├──────┼──────┼──────┼──────┼─┤│
│  │ محمد │ ✅  │ ✅  │ ❌  │ ││
│  │ علي  │ ✅  │ ❌  │ ✅  │ ││
│  │ خالد │ ❌  │ ✅  │ ✅  │ ││
│  └──────┴──────┴──────┴──────┴─┘│
│                                 │
│  إحصائيات الأسبوع:               │
│  ✅ 20 حضور  │ ❌ 4 غياب  │ ⏸ 1 إذن│
│                                 │
│  ┌─────────────────────────┐    │
│  │     📥 تصدير كـ PDF     │    │
│  └─────────────────────────┘    │
│                                 │
├─────────────────────────────────┤
│  [🏠]  [📊]  [👤]  [⚙️]        │
└─────────────────────────────────┘
```

**API:**
- `GET /api/attendance/?circle=1&ordering=-date&page=1` ← سجل الحضور
- `GET /api/attendance/stats/?course=1` ← إحصائيات
- `GET /api/dashboard/absence-report/?course=1&threshold=3` ← إنذارات

### API عند المزامنة

```json
// اتوماتيكياً: POST /api/attendance/batch/
{
  "circle": 1,
  "date": "2026-05-23",
  "records": [
    {"enrollment": 1, "status": "present"},
    {"enrollment": 2, "status": "absent"},
    {"enrollment": 3, "status": "excused"}
  ]
}
```

### API عند الإنترنت متاح (بدون انتظار)

```json
// مباشر: POST /api/attendance/batch/
// يعيد Response فوراً
```

---

## 6. شاشة الحفظ (Batch Memorization)

```
┌─────────────────────────────────┐
│  ← العودة                       │
│  ✏️ حفظ — حلقة الفجر            │
│  📅 [23-05-2026  الخميس]        │
├─────────────────────────────────┤
│                                 │
│  ┌───── طالب: محمد ──────────┐ │
│  │  السورة *  [الفاتحة ▼]    │ │
│  │  من آية [1]  إلى [7]      │ │
│  │  النوع   [✅ جديد] [🔁 مراجعة]││
│  │  النتيجة [🌟] [👍] [🔄]   │ │
│  │  ┌──────────────────┐     │ │
│  │  │  معاينة:          │     │ │
│  │  │  +1 صفحة (+10)    │     │ │
│  │  │  +2 حفظ ممتاز     │     │ │
│  │  └──────────────────┘     │ │
│  └─────────────────────────┘ │
│                                 │
│  ┌───── طالب: علي ───────────┐ │
│  │  السورة *  [البقرة ▼]     │ │
│  │  من آية [1]  إلى [5]      │ │
│  │  ...                       │ │
│  └─────────────────────────┘ │
│                                 │
│  [+ إضافة طالب آخر للحفظ]     │
│                                 │
│  ┌─────────────────────────┐   │
│  │      💾 حفظ الكل        │   │
│  └─────────────────────────┘   │
│                                 │
│  ⚡ محلياً: 2 حفظ - 0 متزامن   │
│                                 │
├─────────────────────────────────┤
│  [🏠]  [📊]  [👤]  [⚙️]        │
└─────────────────────────────────┘
```

### API (عند المزامنة)

```json
// POST /api/memorizations/batch/
{
  "records": [
    {
      "enrollment": 1,
      "surah": 1,
      "from_ayah": 1, "to_ayah": 7,
      "type": "new", "result": "excellent",
      "date": "2026-05-23"
    },
    {
      "enrollment": 2,
      "surah": 2,
      "from_ayah": 1, "to_ayah": 5,
      "type": "new", "result": "good",
      "date": "2026-05-23"
    }
  ]
}
```

### التعديل قبل المزامنة (Edit Before Sync)

```
نفس مبدأ الحضور:
├─ pending: يعدل في SQLite المحلي ← المزامنة ترسل الجديد
└─ synced: ينشئ pending تعديل ← المزامنة ترسل PATCH /api/memorizations/{id}/
           أو ببساطة إعادة إرسال الـ batch (لكنه لن ينشئ duplicate
           لأن MemorizationRecord ليس له unique constraint على (enrollment, date))
           → الحل: نجمع الـ pending edits ونرسل PATCH فردية
```

### سجل الحفظ للحلقة

```
┌─────────────────────────────────┐
│  ← العودة                       │
│  ✏️ سجل الحفظ — حلقة الفجر      │
│  فلترة: [📅 أسبوعي] [شهري] [مخصص]│
├─────────────────────────────────┤
│                                 │
│  إحصائيات سريعة:                 │
│  ✏️ 45 جلسة حفظ │ 🌟 30 ممتاز │
│  👍 10 جيد      │ 🔄 5 إعادة   │
│                                 │
│  ┌──────┬──────────┬────────┬───┐│
│  │تاريخ │ طالب     │ السورة │نتـ││
│  ├──────┼──────────┼────────┼───┤│
│  │19-05 │ محمد    │الفاتحة│🌟 ││
│  │19-05 │ علي     │البقرة │👍 ││
│  │21-05 │ خالد    │الإخلاص│🌟 ││
│  │21-05 │ محمد    │الناس  │🔄 ││
│  └──────┴──────────┴────────┴───┘│
│                                 │
│  [📥 تصدير كـ PDF]               │
│                                 │
├─────────────────────────────────┤
│  [🏠]  [📊]  [👤]  [⚙️]        │
└─────────────────────────────────┘
```

**API:** `GET /api/memorizations/?circle=1&ordering=-recorded_at&page=1`

### Offline Storage (SQLite)

```sql
-- جدول الحفظ المعلق (Pending Memorization)
CREATE TABLE pending_memorizations (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  local_id TEXT UNIQUE,            -- UUID محلي للتعريف
  enrollment_id INTEGER,
  surah_id INTEGER,
  surah_name TEXT,
  from_ayah INTEGER,
  to_ayah INTEGER,
  type TEXT,        -- new / review
  result TEXT,      -- excellent / good / redo
  date TEXT,        -- YYYY-MM-DD
  server_id INTEGER,              -- id بعد المزامنة (nullable)
  sync_status TEXT DEFAULT 'pending',  -- pending | synced | failed
  action TEXT DEFAULT 'create',    -- create | update | delete
  retry_count INTEGER DEFAULT 0,
  created_at TEXT,
  updated_at TEXT
);

-- سجل الحفظ المخبأ محلياً (للعرض السريع)
CREATE TABLE cached_memorizations (
  id INTEGER PRIMARY KEY,
  enrollment_id INTEGER,
  student_name TEXT,
  surah_name TEXT,
  from_ayah INTEGER,
  to_ayah INTEGER,
  type TEXT,
  result TEXT,
  date TEXT,
  recorded_by TEXT
);
```

---

## 7. شاشة طلب السبر (Quiz Request)

```
┌─────────────────────────────────┐
│  ← العودة                       │
│  📝 طلب سبر                     │
├─────────────────────────────────┤
│                                 │
│  الطالب *     [محمد ▼]         │
│  الجزء *      [الجزء الأول ▼]  │
│  النوع *      [✅ جديد]        │
│               [🔁 مراجعة]      │
│  ملاحظات      [______________]│
│                                 │
│  ┌─────────────────────────┐   │
│  │      📤 إرسال الطلب     │   │
│  └─────────────────────────┘   │
│                                 │
│  طلباتي السابقة:                │
│  ┌─────────────────────────┐   │
│  │ محمد - الجزء الأول - ⏳ │   │
│  │ علي  - الجزء الثاني - ✅│   │
│  └─────────────────────────┘   │
│                                 │
├─────────────────────────────────┤
│  [🏠]  [📊]  [👤]  [⚙️]        │
└─────────────────────────────────┘
```

**API:**
- `GET /api/quiz-requests/my_requests` ← طلباتي السابقة
- `POST /api/quiz-requests/` ← إرسال طلب جديد

**Offline:** يمكن حفظ الطلب محلياً ومزامنته لاحقاً.

---

## 8. شاشة سجل الطالب (Student Report)

```
┌─────────────────────────────────┐
│  ← العودة                       │
│  👤 محمد                        │
│  حلقة الفجر — الدورة الصيفية     │
├─────────────────────────────────┤
│                                 │
│  ┌───────┐ ┌───────┐ ┌───────┐ │
│  │ ✅حضور │ │ ✏️ حفظ│ │ 📊سبر│ │
│  │  15   │ │  12   │ │   5  │ │
│  │  حاضر │ │  جلسة │ │ سبرة │ │
│  └───────┘ └───────┘ └───────┘ │
│                                 │
│  ═══════════  الحضور  ═══════  │
│  📅 19-05  ✅ حاضر              │
│  📅 21-05  ❌ غائب              │
│  📅 24-05  ✅ حاضر              │
│                                 │
│  ═══════════  الحفظ  ════════  │
│  📅 19-05  الفاتحة 1-7  🌟     │
│  📅 21-05  البقرة 1-10 👍     │
│                                 │
│  ═══════════  السبرات  ═══════ │
│  📅 15-05  الجزء الأول 90% ✅ │
│  📅 20-05  الجزء الثاني 85% ✅│
│                                 │
│  💰 إجمالي النقاط: 230         │
│  📄 الصفحات المكتملة: 12       │
│                                 │
├─────────────────────────────────┤
│  [🏠]  [📊]  [👤]  [⚙️]        │
└─────────────────────────────────┘
```

**API:** `GET /api/dashboard/student-course-report/?student=:id&course=:id`

**Offline:** يمكن تخزين آخر تقرير مfetched للطالب في SQLite.

---

## 9. شاشة الإحصائيات (Statistics)

```
┌─────────────────────────────────┐
│  ← العودة                       │
│  📊 إحصائيات — حلقة الفجر        │
├─────────────────────────────────┤
│                                 │
│  ┌───────┐ ┌───────┐ ┌───────┐ │
│  │  👥   │ │  ✅   │ │  💰  │ │
│  │  25   │ │  230  │ │ 1,500│ │
│  │ طالب  │ │ حضور  │ │ نقاط  │ │
│  └───────┘ └───────┘ └───────┘ │
│                                 │
│  ═══════════  ترتيب الطلاب  ═══ │
│  ┌───┬──────────┬──────┬─────┐ │
│  │ # │  الاسم   │نقاط  │حضور│ │
│  ├───┼──────────┼──────┼─────┤ │
│  │🥇 │ محمد    │ 230  │ 15  │ │
│  │🥈 │ علي     │ 195  │ 12  │ │
│  │🥉 │ خالد    │ 180  │ 14  │ │
│  │ 4 │ سعيد    │ 160  │ 10  │ │
│  │ 5 │ أحمد    │ 150  │ 11  │ │
│  └───┴──────────┴──────┴─────┘ │
│                                 │
│  ═══════  تقرير الغياب  ══════ │
│  ⚠️ محمد  — 4 غيابات           │
│  ⚠️ سعيد — 3 غيابات            │
│                                 │
├─────────────────────────────────┤
│  [🏠]  [📊]  [👤]  [⚙️]        │
└─────────────────────────────────┘
```

**API:**
- `GET /api/dashboard/teacher-dashboard/?course=1` ← كل شيء
- `GET /api/dashboard/student-ranking/?circle=1&limit=5` ← ترتيب

---

## 10. شاشة الإعدادات (Settings)

```
┌─────────────────────────────────┐
│  ⚙️ الإعدادات                   │
├─────────────────────────────────┤
│  🔄 مزامنة تلقائية  🟢 متصل ✓  │
├─────────────────────────────────┤
│                                 │
│  👤 الملف الشخصي                │
│  ┌─────────────────────────┐   │
│  │  الاسم: الشيخ أحمد      │   │
│  │  الهاتف: 0555000001     │   │
│  │  البريد: a@b.com        │   │
│  └─────────────────────────┘   │
│                                 │
│  ═══════════  المزامنة  ══════ │
│                                 │
│  آخر مزامنة: 22-05-2026 3:00م  │
│                                 │
│  ┌───────┬────────┬──────────┐ │
│  │ النوع │ العدد  │ الحالة   │ │
│  ├───────┼────────┼──────────┤ │
│  │ حضور  │   3    │ 🟡 معلق  │ │
│  │ حفظ   │   2    │ 🟡 معلق  │ │
│  │ سبر   │   0    │ 🟢 متزامن│ │
│  └───────┴────────┴──────────┘ │
│                                 │
│  [🔄 مزامنة الآن]               │
│  [📊 عرض سجل المزامنة]         │
│                                 │
│  ═══════════  الإعدادات  ═════ │
│  ✅ المزامنة التلقائية          │
│  🔔 إشعارات المزامنة           │
│  📶 فقط عبر WiFi للمزامنة      │
│                                 │
│  ═══════════  عن التطبيق  ════ │
│  النسخة: 1.0.0                 │
│  حجم البيانات المخبأة: 2.3 MB  │
│  [🧹 مسح الذاكرة المخبأة]      │
│                                 │
│  [🚪 تسجيل خروج]                │
│                                 │
├─────────────────────────────────┤
│  [🏠]  [📊]  [👤]  [⚙️]        │
└─────────────────────────────────┘
```

### سجل المزامنة (Sync Log)

```
┌─────────────────────────────────┐
│  ← العودة                       │
│  📊 سجل المزامنة                 │
├─────────────────────────────────┤
│                                 │
│  ┌──────────┬────────┬─────────┐│
│  │ الوقت    │ النوع  │ الحالة  ││
│  ├──────────┼────────┼─────────┤│
│  │ 22-05 3م │ حضور×3 │ ✅     ││
│  │ 22-05 2م │ حفظ×2  │ ✅     ││
│  │ 22-05 1م │ حضور×5 │ ✅     ││
│  │ 21-05 6م │ سبر×1  │ ❌ فشل  ││
│  │          │        │ [🔄 إعادة]│
│  └──────────┴────────┴─────────┘│
│                                 │
│  إجمالي المزامنات: 42          │
│  آخر 24 ساعة: 12               │
│                                 │
├─────────────────────────────────┤
│  [🏠]  [📊]  [👤]  [⚙️]        │
└─────────────────────────────────┘
```

**API:** `GET /api/profile/`

---

## 11. استراتيجية المزامنة الخفية الكاملة (Invisible Sync)

> المستخدم العادي لا يعرف حتى بوجود "مزامنة" — كل شيء يعمل تلقائياً.
> التطبيق يخزّن محلياً، يزامن في الخلفية، يعرض دائماً.
> لا شاشة مزامنة، لا أزرار مزامنة، لا تعقيدات.

### المبادئ

```
┌────────────────────────────────────────────────────┐
│               Invisible Sync                        │
│                                                     │
│  1. 📱 المستخدم يضيف حضور/حفظ → حفظ في SQLite فوراً │
│     → التطبيق يرد "تم الحفظ" على الفور              │
│     → المزامنة في الخلفية (غير مرئية)               │
│                                                     │
│  2. 🔄 إذا كان الإنترنت متاحاً: يزامن خلال ثوانٍ    │
│     → بدون إشعار، بدون شريط تحميل                   │
│     → فقط أيقونة صغيرة تتغير (🟢=مزامن، 🟡=معلق)   │
│                                                     │
│  3. 📡 إذا كان الإنترنت غير متاح: يبقى محلياً       │
│     → التطبيق يعمل 100% بدون إنترنت                 │
│     → عند عودة الإنترنت: يزامن تلقائياً بصمت         │
│                                                     │
│  4. 🔋 لا ضغط على البطارية ولا استهلاك بيانات       │
│     → يزامن فقط عند وجود تغييرات                     │
│     → يوقف المزامنة إذا كانت البطارية منخفضة         │
│     → يستخدم batch API (طلب واحد لعدة سجلات)        │
│                                                     │
│  5. 👤 المستخدم العادي يرى:                         │
│     → تطبيق سريع، بياناته محفوظة دائماً              │
│     → لا شاشة "مزامنة"، لا أزرار "حفظ للإنترنت"     │
│     → فقط أيقونة صغيرة 🔵 أو 🟢 أو 🟡 في الزاوية    │
└────────────────────────────────────────────────────┘
```

### أيقونة الحالة الوحيدة (غير مزعجة)

```
في الشريط العلوي (بجانب اسم الحلقة):

  🟢  ← لا شيء معلق، كل شيء متزامن (لا تحتاج فعل شيء)
  🟡  ← يوجد تغييرات محلية (سيتم مزامنتها قريباً)
  🔵  ← جاري المزامنة حالياً (ستختفي بعد ثوانٍ)

بدون نصوص، بدون أرقام، بدون إزعاج.
المستخدم العادي لن يلاحظ حتى وجودها.
```

### استراتيجية خفيفة للمزامنة (Lightweight)

```
1. عند بدء التشغيل:
   → تحميل cached_students + cached_circles من SQLite (فوري)
   → طلب GET /api/enrollments/?circle=1 (في الخلفية)
   → تحديث cache إذا تغير شيء (المستخدم لا يشعر)

2. عند حفظ حضور/حفظ:
   → حفظ في SQLite (1ms)
   → إرسال batch API فوراً إذا كان الإنترنت متاحاً (غير متزامن)
   → إذا فشل: يُضاف إلى طابور ويُرسَل لاحقاً

3. في الخلفية (WorkManager):
   → يعمل مرة كل 30 دقيقة
   → يتحقق: هل هناك pending items؟
   → إذا لا: ينام (0 استهلاك)
   → إذا نعم: يرسل batch → ينام

4. عند عودة الإنترنت (ConnectivityStream):
   → يرسل كل pending items دفعة واحدة (غير متزامن)
   → ينام

5. البطارية والبيانات:
   → يتوقف إذا كانت البطارية < 20%
   → يتوقف إذا المستخدم على roaming
   → لا يرسل أكثر من 50 سجلاً في الدقيقة
```

### كود المزامنة المقترح (خلفي، صامت، سريع)

```dart
class SilentSyncService {
  Timer? _periodicTimer;
  bool _isSyncing = false;

  // --- التهيئة (مرة واحدة عند تشغيل التطبيق) ---
  void init() {
    // 1. استمع لتغيرات الاتصال
    Connectivity().onConnectivityChanged.listen((result) {
      if (result != ConnectivityResult.none) _sync();
    });

    // 2. مزامنة دورية كل 30 ثانية (إذا كان التطبيق مفتوحاً)
    // هذا يعطي انطباعاً فورياً جداً
    _periodicTimer = Timer.periodic(Duration(seconds: 30), (_) {
      if (_hasInternet) _sync();
    });

    // 3. سجل WorkManager للخلفية (حتى لو التطبيق مغلق)
    Workmanager().registerPeriodicTask(
      "silent-sync",
      "silentBackgroundSync",
      frequency: Duration(minutes: 30),  // اقتصادي
      constraints: Constraints(
        networkType: NetworkType.CONNECTED,
        batteryNotLow: true,
      ),
    );

    // 4. مزامنة فورية عند بدء التشغيل (إذا كان هناك إنترنت)
    if (_hasInternet) _sync();
  }

  // --- المزامنة الصامتة (دون إزعاج المستخدم) ---
  Future<void> _sync() async {
    if (_isSyncing) return;  // تجنب التكرار
    _isSyncing = true;

    try {
      final pending = await _db.query('sync_queue',
        where: 'sync_status = ?',
        whereArgs: ['pending'],
        limit: 50,  // لا ترسل أكثر من 50 في المرة
      );

      if (pending.isEmpty) return;  // لا شيء تفعله — نم

      // جمّع حسب الـ type للإرسال batch
      final attendance = pending.where((e) => e['entity_type'] == 'attendance').toList();
      final memorizations = pending.where((e) => e['entity_type'] == 'memorization').toList();
      final requests = pending.where((e) => e['entity_type'] == 'quiz_request').toList();

      // أرسل كل نوع معاً
      if (attendance.isNotEmpty) await _sendAttendanceBatch(attendance);
      if (memorizations.isNotEmpty) await _sendMemorizationBatch(memorizations);
      if (requests.isNotEmpty) await _sendQuizRequests(requests);

      // سحب آخر التحديثات من السيرفر
      await _fetchLatest();
    } catch (_) {
      // فشل المزامنة — سنحاول لاحقاً
      // لا نزعج المستخدم
    } finally {
      _isSyncing = false;
    }
  }

  Future<void> _sendAttendanceBatch(List items) async {
    // جمّع حسب (date, circle)
    // أرسل POST /api/attendance/batch/
    // علام كـ synced عند النجاح
  }

  Future<void> _fetchLatest() async {
    // GET /api/enrollments/?circle=... → تحديث cached_students
    // GET /api/attendance/?circle=... → تحديث cached_attendance
    // GET /api/memorizations/?circle=... → تحديث cached_memorizations
  }
}

// في main.dart:
void main() {
  WidgetsFlutterBinding.ensureInitialized();
  Workmanager().initialize(callbackDispatcher);
  runApp(MyApp());
}

@pragma('vm:entry-point')
void callbackDispatcher() async {
  WidgetsFlutterBinding.ensureInitialized();
  await SilentSyncService().sync();  // صامت، بدون واجهة
}
```

### ملخص: كيف يرى المستخدم التطبيق

```
┌────────────────────────────────────────────┐
│                                            │
│  ما يراه المستخدم:                         │
│  ✓ تطبيق سريع — يسجل الحضور فوراً          │
│  ✓ يعمل بدون إنترنت — في المصعد، في البيت │
│  ✓ كل شيء محفوظ — لا يفقد بيانات           │
│  ✓ يفتح التطبيق ويجد كل طلابه وبياناتهم    │
│                                            │
│  ما لا يراه المستخدم:                      │
│  ✗ لا يعرف حتى بوجود "مزامنة"             │
│  ✗ لا يرى شاشات تحميل أو انتظار           │
│  ✗ لا يضغط أزرار "حفظ" أو "إرسال"        │
│  ✗ لا يعرف معنى pending أو synced         │
│                                            │
│  أيقونة وحيدة يراها (لكن قد لا ينتبه):    │
│  🟢 أو 🟡 صغيرة في أعلى الشاشة            │
│                                            │
└────────────────────────────────────────────┘
```

---

## 12. نظام Offline-First (المزامنة) — معماري متقدم

### الهيكل

```
التطبيق (Flutter)
    │
    ├─ [Online] → API مباشر → Django Backend
    │
    └─ [Offline] → SQLite محلي
                      │
                      ├─ جدول الطلاب (cached_students)
                      ├─ جدول الحلقات (cached_circles)
                      ├─ جدول الحضور المعلق (pending_attendance)
                      ├─ جدول الحفظ المعلق (pending_memorizations)
                      └─ جدول طلبات السبر المعلقة (pending_quiz_requests)
```

### تدفق المزامنة

```
1. التطبيق يفتح → يتحقق من الاتصال بالإنترنت
2. إذا متصل:
   a. يرسل جميع البيانات المعلقة (pending) إلى الـ API
   b. يحذف المعلق من SQLite بعد النجاح
   c. يسحب آخر التحديثات من الـ API (الطلاب، الحلقات، إلخ)
   d. يخزنها في SQLite المحلي
3. إذا غير متصل:
   a. يعمل فقط من SQLite المحلي
   b. يحفظ أي إجراء جديد في جداول pending
   c. يعرض شريط "غير متصل — التغييرات محفوظة محلياً"
4. عند عودة الاتصال:
   a. المتزامن (SyncService) يعمل تلقائياً
   b. يرسل pending data → API
   c. يحدث local DB
   d. يعرض "تمت المزامنة"
```

### الجداول المحلية (SQLite)

```sql
-- الطلاب المخبئون
CREATE TABLE cached_students (
  id INTEGER PRIMARY KEY,
  enrollment_id INTEGER,
  name TEXT,
  circle_id INTEGER,
  course_id INTEGER,
  circle_name TEXT,
  status TEXT
);

-- الحضور المعلق
CREATE TABLE pending_attendance (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  enrollment_id INTEGER,
  date TEXT,
  status TEXT,       -- present / absent / excused
  circle_id INTEGER,
  created_at TEXT
);

-- الحفظ المعلق
CREATE TABLE pending_memorizations (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  enrollment_id INTEGER,
  surah_id INTEGER,
  surah_name TEXT,
  from_ayah INTEGER,
  to_ayah INTEGER,
  type TEXT,
  result TEXT,
  date TEXT,
  created_at TEXT
);

-- طلبات السبر المعلقة
CREATE TABLE pending_quiz_requests (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  enrollment_id INTEGER,
  quran_part_id INTEGER,
  quiz_type TEXT,
  teacher_notes TEXT,
  created_at TEXT
);
```

### معضلة الإنترنت الوهمي (Fake Internet / Intermittent Connection)

المشكلة: المستخدم في مصعد أو نفق — الإنترنت يظهر ويختفي كل ثانية.
الحل: **Exponential Backoff + Queue + State Machine**.

```
الحالة: غير متصل
    │
    ▼
ConnectivityStream يكتشف اتصالاً
    │
    ▼
الحالة: متصل — بدء المزامنة
    │
    ├─ إرسال batch 1 → نجاح ✓
    ├─ إرسال batch 2 → فشل ✕ (انقطع الاتصال)
    │
    ▼
الحالة: غير متصل مرة أخرى
    │
    ▼
بعد 5 ثوانٍ → إعادة المحاولة (retry 1)
بعد 25 ثانية → إعادة المحاولة (retry 2) 
بعد 125 ثانية → إعادة المحاولة (retry 3)
... (exponential backoff: 5, 25, 125, 625, ...)
    │
    ▼
بعد 5 محاولات فاشلة → يصبح failed
    │
    ▼
يعلم المستخدم: "⚠️ فشلت المزامنة لـ 3 عناصر — اضغط لإعادة المحاولة"
```

### الـ State Machine لكل Pending Item

```
                  ┌──────────┐
         إنشاء ← │  PENDING │
                  └──────────┘
                       │
                  بدء المزامنة
                       │
                       ▼
                  ┌──────────┐
                  │ SENDING  │ ← جاري الإرسال حالياً
                  └──────────┘
                    │        │
                  نجاح      فشل
                    │        │
                    ▼        ▼
              ┌────────┐ ┌──────────┐
              │ SYNCED │ │ PENDING  │ ← retry_count++
              └────────┘ │          │
                         │ retry > 5│
                         │          ▼
                         │ ┌──────────┐
                         │ │ FAILED   │ ← يحتاج تدخل المستخدم
                         │ └──────────┘
                         │    │
                         │  المستخدم اختار "إعادة"
                         │    │
                         └───┘ (retry_count = 0)
```

### الصراع (Conflict Resolution)

عند تعديل سجل من جهازين مختلفين (نادر لتطبيق المدرس):

```
السيناريو: المدرس سجّل محمد "حاضر" على هاتفه
           مدير الحلقة سجّل محمد "غائب" على جهاز آخر
           
عند المزامنة:
  → كلا الجهازين يرسلان POST /api/attendance/batch/
  → السيرفر يستخدم get_or_create → آخر من يرسل هو من "يفوز"
  → (Last-Write-Wins)

الحل البديل (للمستقبل):
  → إضافة updated_at + timestamp
  → المقارنة: أحدث timestamp يفوز
  → إعلام المستخدم بالصراع إذا كان الفرق < 5 دقائق
```

### ملخص تدفق المزامنة الكامل

```
┌──────────────────────────────────────────────────────────┐
│                     SyncService                          │
│                                                          │
│  syncAll():                                              │
│  ┌──────────────────────────────────────────────────┐   │
│  │ 1. syncAttendance()                              │   │
│  │    → جمع كل pending_attendance                    │   │
│  │    → تجميع حسب (date, circle)                    │   │
│  │    → إرسال batch API                             │   │
│  │    → تحديث sync_status                           │   │
│  ├──────────────────────────────────────────────────┤   │
│  │ 2. syncMemorizations()                           │   │
│  │    → جمع كل pending_memorizations                 │   │
│  │    → تجميع حسب date                              │   │
│  │    → إرسال batch API                             │   │
│  │    → تحديث sync_status                           │   │
│  ├──────────────────────────────────────────────────┤   │
│  │ 3. syncQuizRequests()                            │   │
│  │    → إرسال فردي (ليس له batch)                   │   │
│  ├──────────────────────────────────────────────────┤   │
│  │ 4. fetchLatestData()                             │   │
│  │    → GET /api/enrollments/?circle=1              │   │
│  │    → تحديث cached_students                       │   │
│  │    → GET /api/attendance/?circle=1&ordering=-date│   │
│  │    → تحديث cached_attendance                     │   │
│  │    → GET /api/memorizations/?circle=1            │   │
│  │    → تحديث cached_memorizations                  │   │
│  └──────────────────────────────────────────────────┘   │
│                                                          │
│  المرات التي يُستدعى فيها:                               │
│  • فوراً: بعد كل حفظ من المستخدم                        │
│  • دورياً: WorkManager كل 15 دقيقة                      │
│  • عند عودة الاتصال: ConnectivityStream                  │
│  • يدوياً: المستخدم يضغط "مزامنة الآن"                   │
└──────────────────────────────────────────────────────────┘
```

### الكود (SyncService)

```dart
class SyncService {
  static const int MAX_RETRIES = 5;

  Future<SyncResult> syncAll() async {
    final result = SyncResult();

    await Future.wait([
      _syncAttendance(result),
      _syncMemorizations(result),
      _syncQuizRequests(result),
    ]);

    if (result.successCount > 0) await _fetchLatestData();
    return result;
  }

  Future<void> _syncAttendance(SyncResult result) async {
    final pending = await _db.query(
      'pending_attendance',
      where: 'sync_status = ? AND retry_count < ?',
      whereArgs: ['pending', MAX_RETRIES],
    );
    if (pending.isEmpty) return;

    final groups = _groupBy(pending, (e) => '${e['date']}|${e['circle_id']}');

    for (final group in groups.values) {
      await _db.update('pending_attendance',
        {'sync_status': 'sending'},
        where: 'id IN ${_ids(group)}',
      );

      try {
        final records = group.map((e) => {
          'enrollment': e['enrollment_id'],
          'status': e['status'],
        }).toList();

        await _api.post('/api/attendance/batch/', {
          'circle': group.first['circle_id'],
          'date': group.first['date'],
          'records': records,
        });

        await _db.update('pending_attendance',
          {'sync_status': 'synced'},
          where: 'id IN ${_ids(group)}',
        );
        result.successCount += group.length;
      } catch (e) {
        await _db.update('pending_attendance',
          {'sync_status': 'pending',
           'retry_count': group.first['retry_count'] + 1},
          where: 'id IN ${_ids(group)}',
        );
        result.failCount += group.length;
      }
    }
  }

  Future<void> _fetchLatestData() async {
    // تحديث الطلاب المخبئين من السيرفر
    final students = await _api.get('/api/enrollments/?circle=$_currentCircleId');
    await _db.delete('cached_students');
    for (final s in students['results']) {
      await _db.insert('cached_students', s);
    }
  }

  List<Map<String, dynamic>> _groupBy(List list, String key(dynamic)) {
    final map = <String, List>{};
    for (final item in list) {
      final k = key(item);
      map.putIfAbsent(k, () => []).add(item);
    }
    return map.values.toList();
  }

  String _ids(List list) {
    final ids = list.map((e) => e['id']).join(',');
    return '($ids)';
  }
}

class SyncResult {
  int successCount = 0;
  int failCount = 0;
  bool get hasPending => failCount > 0;
}
```

### هيكل قاعدة SQLite المحلية

```sql
-- جدول المزامنة الأساسي (Sync Queue)
CREATE TABLE sync_queue (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  local_id TEXT UNIQUE,            -- UUID
  entity_type TEXT,                -- attendance / memorization / quiz_request
  action TEXT DEFAULT 'create',    -- create / update / delete
  server_id INTEGER,               -- id في السيرفر (null قبل المزامنة)
  payload TEXT,                    -- JSON كامل للبيانات
  sync_status TEXT DEFAULT 'pending',  -- pending / sending / synced / failed
  retry_count INTEGER DEFAULT 0,
  last_error TEXT,
  created_at TEXT,
  updated_at TEXT
);

-- مؤشرات للبحث السريع
CREATE INDEX idx_sync_status ON sync_queue(sync_status);
CREATE INDEX idx_entity ON sync_queue(entity_type, server_id);
```

### كيف يتعامل التطبيق مع إغلاق كامل (App Killed)

```
1. Android: WorkManager يستخدم AlarmManager + JobScheduler
   → حتى لو قُتل التطبيق، Android يستيقظ في الوقت المحدد
   → يشغل callbackDispatcher()
   → يرسل البيانات المعلقة
   → ينتهي

2. iOS: BGTaskScheduler
   → iOS يقرر متى يشغل المهام الخلفية (ليس للمطور تحكم كامل)
   → لكن التطبيق يمكنه طلب تشغيل عاجل (BGAppRefreshTaskRequest)
   → في المتوسط: كل 15-30 دقيقة

3. الحل الأكثر ضماناً: **Push Notification + Silent Data**
   → السيرفر يرسل silent push → التطبيق يستيقظ → يزامن
   → هذا يضمن المزامنة فوراً حتى مع التطبيق مغلق
   → لكنه يحتاج FCM (Firebase Cloud Messaging) + Server infrastructure
```

---

## 13. قائمة API المستخدمة في تطبيق المدرس

| الغرض | API | الطريقة |
|-------|-----|---------|
| تسجيل دخول | `/api/token/` | POST |
| الملف الشخصي | `/api/profile/` | GET |
| قائمة الحلقات | `/api/circles/` | GET |
| طلاب الحلقة | `/api/enrollments/?circle=:id` | GET |
| بحث طالب | `/api/students/?search=:name` | GET |
|---|---|---|
| حضور دفعة | `/api/attendance/batch/` | POST |
| حضور فردي | `/api/attendance/` | POST |
| حفظ دفعة | `/api/memorizations/batch/` | POST |
| حفظ فردي | `/api/memorizations/` | POST |
| طلب سبر | `/api/quiz-requests/` | POST |
| طلباتي | `/api/quiz-requests/my_requests` | GET |
|---|---|---|
| تقرير شامل | `/api/dashboard/student-course-report/` | GET |
| ترتيب طلابي | `/api/dashboard/student-ranking/?circle=:id` | GET |
| إحصائيات الحضور | `/api/attendance/stats/?course=:id` | GET |
|---|---|---|
| لوحة المدرس | `/api/dashboard/teacher-dashboard/` | GET |
|---|---|---|
| **جميع النهايات مع `?course=`, `?circle=`, `?search=`, `?ordering=`** |

---

## 14. خريطة التنقل (Navigation)

```
تسجيل دخول
    │
    ▼
الشاشة الرئيسية (Home)
    │
    ├─【اختيار دورة + حلقة】─▶ تظهر أزرار الإجراءات
    │
    ├─▶ حضور/غياب (دفعة)
    │       │
    │       ├─ اختيار التاريخ
    │       ├─ اختيار الحالة لكل طالب
    │       └─ حفظ (محلي → مزامنة)
    │
    ├─▶ حفظ (دفعة)
    │       │
    │       ├─ اختيار التاريخ
    │       ├─ إدخال بيانات الحفظ لكل طالب
    │       └─ حفظ (محلي → مزامنة)
    │
    ├─▶ طلب سبر
    │       │
    │       ├─ اختيار الطالب + الجزء
    │       └─ إرسال (محلي أو مباشر)
    │
    ├─▶ سجل طالب
    │       │
    │       ├─ اختيار طالب
    │       └─ عرض التقرير الشامل
    │
    ├─【سجل الحضور】─▶ سجل حضارة الحلقة مع فلترة
    │
├─【سجل الحفظ】─▶ سجل حفظ الحلقة مع فلترة
    │
├─【طلابي】─▶ قائمة طلاب + ترتيب (أبجدي/نقاط/حضور) + بحث + تفاصيل
    │
├─【سجل الحضور】─▶ سجل حضور الحلقة مع فلترة
    │
├─【سجل الحفظ】─▶ سجل حفظ الحلقة مع فلترة
    │
    ├─【طلابي】─▶ قائمة طلاب + ترتيب + بحث
    │       │
    │       └─【اختيار طالب】─▶ سجل الطالب الشامل
    │
    ├─【سجل الحضور】─▶ سجل حضور الحلقة
    │
    ├─【سجل الحفظ】─▶ سجل حفظ الحلقة
    │
    ├─【الإحصائيات】─▶ عرض ترتيب + غياب
    │
    └─【الإعدادات】─▶ ملف شخصي + تسجيل خروج
```

---

## 15. الـ Bottom Navigation Bar

```
┌──────────┬──────────┬──────────┬──────────┐
│   🏠     │   📊    │   👤    │   ⚙️    │
│ الرئيسية │ إحصائيات │ طلاب    │ إعدادات │
└──────────┴──────────┴──────────┴──────────┘
```

- **🏠 الرئيسية:** اختيار الدورة/الحلقة + أزرار الإجراءات
- **📊 إحصائيات:** ترتيب الطلاب + تقارير سريعة
- **👤 طلاب:** قائمة بجميع طلاب الحلقة — بحث — سجل كل طالب
- **⚙️ إعدادات:** ملف شخصي + مزامنة + تسجيل خروج

---

## 16. الألوان والهوية البصرية

| العنصر | اللون | الاستخدام |
|--------|-------|-----------|
| الخلفية الرئيسية | `#F8FAFC` | خلفية التطبيق |
| الشريط العلوي | `#059669` (emerald-600) | Header + أزرار رئيسية |
| النصوص | `#1E293B` (slate-800) | نصوص عادية |
| ✅ حاضر | `#16A34A` (green-600) | حضور |
| ❌ غائب | `#DC2626` (red-600) | غياب |
| ⏸ إذن | `#F59E0B` (amber-500) | اعتذار |
| 🌟 ممتاز | `#F59E0B` (gold) | حفظ ممتاز |
| 👍 جيد | `#16A34A` (green) | حفظ جيد |
| 🔄 إعادة | `#F97316` (orange) | حفظ إعادة |
| الخط | Cairo / Tajawal | خط عربي واضح |

```
📱 متطلبات الشاشة: API Level 21+ (Android 5.0)
📦 الحجم التقريبي: 8-12MB
💾 حجم البيانات المحلية: 1-5MB (يعتمد على عدد الطلاب)
```